## infrastructure/code
- Look through the java files and identify code duplication.

<This is the first task.>


- Modify the gradlew run command to take in an optional faiss file.
- Modify the gradlew e2e script to take either "cluster/BP/Gorder" as the first argument
- Move the test cases into the test directory

## debugging
Debug the low recall with the modified files.